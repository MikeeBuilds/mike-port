
const links = [
    {path: '/', name: 'home'}
    const Nav = ({containerStyles, linkStyles, underlineStyles}) => {
      return   <nav className={`${containerStyles}`}>Nav</nav>
    
    }
    
    export default Nav
]


const Nav = ({containerStyles, linkStyles, underlineStyles}) => {
  return   <nav className={`${containerStyles}`}>Nav</nav>

}

export default Nav